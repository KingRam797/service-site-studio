# Optional: POST receives the validated inquiry JSON.
INQUIRY_WEBHOOK_URL=

# Optional: send owner notifications through Resend's HTTP API.
RESEND_API_KEY=
INQUIRY_FROM_EMAIL=

# Optional canonical site URL.
NEXT_PUBLIC_SITE_URL=

# Client authentication (Clerk via Vercel Marketplace).
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in

# Client projects and milestone state (Neon Postgres).
DATABASE_URL=

# Authenticated milestone checkout and signed payment confirmation.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
